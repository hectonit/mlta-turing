# Интерпретатор машины Тьюринга

## Синтаксис

Правила, которые будут подаваться на вход интерпретатору, должны выглядеть следующим образом:
```
<state>,<symbol> -> <new_state>,<new_symbol>,<move>
```

`state` и `new_state` - старое и новое состояния (строки из любых нормальных ascii символов кроме пробельных и `,`). 

`symbol` и `new_symbol` - старый и новый символы, записанные на ленте (требования те же, что и к состояниям)

`move` - `L`, `R`, или `N` (то, куда сдвинется указатель).

Изначально машина находится в состоянии `_start`, когда машина пришла в состояние `_finish` вычисление считается завершенным. 
Изначально указатель расположен в начале входа. 
Во всех остальных ячейках ленты записано `#`.
Результатом исполнения считается строка от позиции указателя в состоянии `_finish` до первой `#`.
Если машина не завершила вычисление, то будет ответ `Not finished`.

Можно оставлять пустые строки для выделения логических блоков кода.

Примеры кода есть в `examples`.

## Как запустить

- Склонировать репозиторий

```
python main.py <filename>
```

`filename` - имя файла с кодом.

Далее программа будет ожидать вход: строку символов, все символы разделены пробелами!!! (для поддержки символов из нескольких букв).

### Пример
```
python main.py examples/inverter
# или так
uv run main.py examples/inverter

stdin:
0 1 0 1

stdout:
1 0 1 0
```

### Пояснения к примерам
`pattern_while_true`: пример бесконечного цикла
```
_start,* -> loop,#,L // переходим из стартового состояния в независимости от символа в loop, и меняем символ на #, идем влево
loop,* -> loop,#,L // идем влево в независимости от символа, меняя все на #
```
`inverter`: инвертируем бинарную строку
```
// задаем начальный переход
_start,0 -> inv,1,R
_start,1 -> inv,0,R
// идем вправо меняя символы
inv,0 -> inv,1,R
inv,1 -> inv,0,R
// дошли до # - идем обратно
inv,# -> back,#,L
back,0 -> back,0,L
back,1 -> back,1,L
// дошли до начала - меняем состояние на _finish, вычисление закончено
back,# -> _finish,#,R
```
`double`: удваиваем бинарную строку
```
// начальный переход, carry0 - несем вперед 0, carry1 - несем вперед 1, (0p, 1p) - помечаем числа, что они уже обработаны
_start,0 -> carry0,0p,R
_start,1 -> carry1,1p,R

// идем вправо, сохраняя все символы
carry0,0 -> carry0,0,R
carry0,1 -> carry0,1,R
carry0,0f -> carry0,0f,R
carry0,1f -> carry0,1f,R
// дошли до конца - ставим 0 с пометкой и идем назад
carry0,# -> back,0f,L

// то же самое для 1
carry1,0 -> carry1,0,R
carry1,1 -> carry1,1,R
carry1,0f -> carry1,0f,R
carry1,1f -> carry1,1f,R
carry1,# -> back,1f,L

// возвращаемся назад до обработанных символов
back,0f -> back,0f,L
back,1f -> back,1f,L
back,0 -> back,0,L
back,1 -> back,1,L
// заодно уже сразу уберем пометки
back,0p -> next,0,R
back,1p -> next,1,R

// если следующий символ необработанный то переносим его
next,0 -> carry0,0p,R
next,1 -> carry1,1p,R
// иначе мы все перенесли, надо очистить символы от пометок
next,0f -> clean,0,R
next,1f -> clean,1,R

// убираем пометки
clean,0f -> clean,0,R
clean,1f -> clean,1,R
clean,# -> final_back,#,L

// просто идем назад
final_back,0 -> final_back,0,L
final_back,1 -> final_back,1,L
final_back,# -> _finish,#,R
```


### Patterns

В описании символов можно использовать `*`.
В символе нельзя использовать более одной `*`. Проверка на соответствие происходит следующим образом:
- если в инструкции нет `*`, то происходит обычное сравнение (если такая есть, она приоритетнее, чем инструкция,
использующая `*` в независимости от порядка инструкций)
- если в инструкции в старом символе есть `*`, а в новом нет, то используется инструкция, удовлетворяющая шаблону
- если в инструкции и в старом, и в новом символе есть `*`,
то используется инструкция, удовлетворяющая шаблону и в правый символ вместо `*` подставляется соответствующая часть.

Это полезно тем, что теперь можно писать такого рода инструкции (идем вправо независимо от символа):
```
state,* -> state,*,R
state,# -> _finish,#,N
```

### Тесты
```
uv sync
uv run python -m pytest tests
```
